#include<stdio.h>
int main(){
    int a,b,c,sum;
    printf("Enter the three no.");
    scanf("%d%d%d",&a,&b,&c);
    if((a>=10||a<=20)||(b>=10||b<=20)||(c>=10||c<=20)){
        a==0;
        b==0;
        c==0;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
   
    else if(a=13)
    {
         a=13;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
      else if(b=13)
    {
         b=13;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
      else if(c=13)
    {
         c=13;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
    else if(a=17)
    {
         a=17;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
      else if(b=17)
    {
         b=17;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
      else if(c=17)
    {
         c=17;
        sum=a+b+c;
        printf("Sum is %d",sum);
    }
    else{
        sum=a+b+c;
        printf(" sum is %d",sum);
    }
}